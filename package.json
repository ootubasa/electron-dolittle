{
  "name": "electron-dolittle",
  "version": "0.0.1",
  "description": "electron-dolittle",
  "main": "main.js",
  "private": true,
  "scripts": {
    "test": "npx electron  ./",
    "build": "npx electron-packager ./ Dolittle --platform=win32 --arch=x64 --icon=favicon.ico --overwrite",
    "rebuild": "npx electron-rebuild",
    "noble-rebuild": "npm rebuild --runtime=electron --target=3.0.10 --arch=x64 --rebuild --disturl=https://atom.io/download/electron --build_from_source=true"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "electron": "^3.0.10",
    "electron-packager": "^12.2.0",
    "electron-rebuild": "^1.8.2"
  },
  "dependencies": {
    "bbc-microbit": "^0.2.0",
    "bootstrap-honoka": "^4.1.3",
    "brace": "^0.9.1",
    "jquery": "^3.3.1",
    "noble-uwp": "^0.6.2",
    "serialport": "^7.1.0"
  }
}
